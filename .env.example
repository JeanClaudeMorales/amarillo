# ============================================================
# Amarillo WiFi — Variables de Entorno
# Copia este archivo a .env y completa los valores
# cp .env.example .env
# ============================================================

# --- BASE DE DATOS MySQL ---
DB_HOST=localhost
DB_PORT=3306
DB_NAME=emily
DB_USER=mysql
DB_PASS=12345678

# --- SERVIDOR API ---
API_PORT=3001

# Secreto para el hash de contraseñas.
# ¡CAMBIA ESTO EN PRODUCCIÓN!
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PASSWORD_SALT=cambia_este_valor_en_produccion

# --- FRONTEND VITE (solo desarrollo local) ---
VITE_PORT=3000
VITE_API_TARGET=http://localhost:3001

# --- GEMINI AI (opcional) ---
GEMINI_API_KEY=
APP_URL=
